# Knock-bead fluorescence decoder — Python dependencies
# Install with: pip install -r requirements.txt

flask>=3.0                # Web UI server
tifffile>=2023.1.1        # Reading multichannel TIFF microscopy images
numpy>=1.24               # Array math
pandas>=2.0               # CSV output
scikit-image>=0.21        # Blob detection (blob_log) + Otsu threshold
matplotlib>=3.7           # Visualisation
scikit-learn>=1.3         # Optional: GMM-based thresholding (--threshold-method gmm)
